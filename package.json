{
  "name": "urlcolors",
  "version": "3.1.0",
  "private": true,
  "type": "module",
  "description": "Chrome Extension to highlight the body of a webpage with colored border for URL's that match user settings.",
  "scripts": {
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "@eslint/js": "^9.15.0",
    "eslint": "^9.15.0",
    "globals": "^15.12.0",
    "prettier": "^3.3.3",
    "eslint-config-prettier": "^9.1.0"
  }
}
